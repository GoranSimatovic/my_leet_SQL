SELECT user_id, CONCAT(UPPER(LEFT(name,1)),LOWER(SUBSTRING(name,2))) AS 'name'
FROM Users
ORDER BY 1;



/*
SELECT user_id,
    CONCAT(
            UCASE(LEFT(name,1)),
            LCASE(SUBSTRING(name,2))
    ) as name
FROM Users
ORDER BY 1;
*/
